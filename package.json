{
  "name": "new-eazy-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
